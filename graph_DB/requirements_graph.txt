# requirements_graph.txt
# Dependencies cho graph_DB pipeline (xây graph + export JSON/Cypher)
#
# Cài trong Colab:
#   !pip install -r requirements_graph.txt -q
#
# neo4j driver KHÔNG bắt buộc - chỉ cần khi import vào AuraDB sau
# Để cài thêm khi có Neo4j: pip install neo4j>=5.14.0

# LLM inference
transformers>=4.45.0
accelerate>=0.26.0
bitsandbytes>=0.42.0   # 4-bit quantization cho Colab T4

# Document parsing
python-docx>=1.1.0

# Utilities
tqdm>=4.66.0

# Optional: chỉ cần khi import vào Neo4j AuraDB
# neo4j>=5.14.0
